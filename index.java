import java.util.Scanner;
find out if agiven character is a vower or conconant using index of function 

write a program to extract last name from the name and 
extract only denominator from a string containing both amount and currency symbol

class index
{
    public static void main(string args[]){
           Scanner sc = new Scanner(System.in);
           string s1 = sc.nextLine();
           for(int i = 0 ; i<s1.length() ; i++){
               if(s1[i]==" "){
               System.out.println(s1.substring(i));
               }
}
               
       
           
